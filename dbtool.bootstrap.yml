# ---------------------------------------------------------------------------
# dbtool.bootstrap.yml   – service catalogue
#
# * The optional `path:` must be a DIRECTORY, not an individual .sql file.
#   dbtool will create `db/migrations/` and `db/schema/` inside it when needed.
# ---------------------------------------------------------------------------

services:
  maxlove:
    database: maxlove_db
    user:     root
    password: Password1
    privileges: ALL
    # path: ~/dev/madmaxbot/madmaxbot-svc-lovemax/db/

  auth:
    database: authguard_db
    user:     authguard_user
    password: Password2
    privileges: ALL
    # default layout will be used unless you uncomment the line below
    # path: backend/auth-service
